const pontuacaoUsuario = 100;
// mesma coisa que usar if e else //
const nivel = pontuacaoUsuario >= 1000 ? 'usuario vip' : 'usuario normal'

const corU = null
const corP = corU || 'preta'


console.log(nivel, corP)